mixin errors(errors)
  if errors
      div(class="alert alert-warning" role="alert")
          each error in errors
              each constraint in error.constraints
                  | #{__(constraint)}
                  br
          else
              | #{__(errors.message)}
